using UnityEngine;
using System.Collections;

public class SetControllerParticleMesh : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //GetComponent<ParticleSystem>().shape.mesh = transform.parent.GetComponent<MeshFilter>().mesh;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
